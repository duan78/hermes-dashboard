# Terminal WebSocket is handled directly in main.py with IP verification.
# This module is intentionally left empty — the /ws/terminal endpoint
# registered here was an unprotected duplicate and has been removed.
